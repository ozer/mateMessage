import React from "react";
import StyledView from "./StyledView";
import { View } from "react-native";

const Separator = () => {
  return <View style={{ minHeight: 36, padding: 8 }} />;
};

export default Separator;
